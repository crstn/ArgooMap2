package com.mapitz.gwt.googleMaps.client;

class GLogImpl {
    /**
     * write(message, color?) none Writes the message as plain text into the log
     * window. HTML markup characters will be escaped so that they are visible
     * as characters.
     * 
     * @param message
     * @param color
     */
    public static native void write(String message, String color)/*-{
     $wnd.GLog.write(message, color);
     }-*/;

    /**
     * write(message, color?) none Writes the message as plain text into the log
     * window. HTML markup characters will be escaped so that they are visible
     * as characters.
     * 
     * @param message
     * @param color
     */
    public static native void write(String message)/*-{
     $wnd.GLog.write(message);
     }-*/;

    /**
     * writeUrl(url) none Writes a link to the given URL into the log window.
     * 
     * @param url
     */
    public static native void writeUrl(String url)/*-{
     $wnd.GLog.writeUrl(url);
     }-*/;

    /**
     * writeHtml(html) none Writes text as HTML in the log window.
     * 
     * @param html
     */
    public static native void writeHtml(String html)/*-{
     $wnd.GLog.writeHtml(html);
     }-*/;
}
